# Keep this file as a template only. Never place the real API key in client-side VITE_ variables.
OPENAI_API_KEY=your_openai_api_key_here
# Optional: change this if your OpenAI project uses a different available model.
OPENAI_MODEL=gpt-5.6-terra
